<% for (const key in enhanceFiles) { %>
import <%- "pluginModule_" + key %> from '<%- type === "client" ? enhanceFiles[key].client : enhanceFiles[key].server %>'
<% } %>

export default function (options) {
  <% for (const key in enhanceFiles) { %>
    <%- "pluginModule_" + key %>(options)
  <% } %>
}